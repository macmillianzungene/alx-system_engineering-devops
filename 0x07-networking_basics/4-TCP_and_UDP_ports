#!/usr/bin/env bash
# This script displays listening ports and the PID/program name for each socket

# Use netstat to get the information
# -l lists only listening sockets
# -t shows TCP connections
# -u shows UDP connections
# -n shows numerical addresses
# -p shows the PID and name of the program to which each socket belongs
sudo netstat -ltnup

